OVERVIEW

L11 is a language of analyzing JavaScript ASTs (e.g. L7) to provide static information
about relationships between nodes.

L11 is written in L0. 

BUILDING

To build:

   cd build
   ./make.sh

This produces the file:

   ../bin/l11c.swf


USING

To run:

    ./comp.sh some.js.l7

or

    avmshell ../bin/l11c.swf some.js.l7

where avmshell is the name of a recent build of Tamarin.

This produces the file:

    some.js.l7.l11

